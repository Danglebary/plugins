---
status: open
depends_on: [002]
---

# Defect fixes and absorbed behavior edits

## Goal

The verified defect list from the nine-axis comparison is resolved, and the three grilled-in behavior edits land: the ticket-start research opener runs unconditionally with its config axis cut, `/done`'s closing fork reads refactor-pass-centric with a judgment recommendation, and the plugin's CLAUDE.md carries the corpus-wide recommendation-with-reasoning authoring rule. These land before the slimming fleet so its clause ledgers capture corrected contracts.

## Acceptance criteria

- [ ] `/done` and `/retro` agree on deleted-branch closure: both verify positively (the close commit located via git history), and no arm infers "fully closed" from a branch's absence.
- [ ] No shipped plugin prose cites this repo's ADR numbers; the placement test is stated where it lives in the plugin's own docs and cited from there.
- [ ] Every inlined store-artifact path list includes the config file, fanned out to every copy per the sync-set consequence.
- [ ] The plugin CONTEXT.md describes the serialized single-active reality; concurrent-open specs and routine manual pointer repointing are no longer presented as normal.
- [ ] The dangling CONTEXT-MAP reference is gone.
- [ ] Abandoning a spec is a documented manual recipe with exactly one home; skills that mention abandonment point there.
- [ ] `/to-spec` requires reading the named modules before freezing Modules touched.
- [ ] The research opener is unconditional: the `ticket_start.research_opener` key is gone from setup's provisioning and the config doc, and the opener prose carries no conditional.
- [ ] `/done`'s closing fork presents "run the refactor pass, or don't" and recommends one arm based on the nature of the diff — code touching seams or multiple modules recommends the pass; doc-only or output-capture work recommends skipping to the merge — never without reasoning. The fork remains a blocking gated offer and the merge mechanics are unchanged.
- [ ] The plugin CLAUDE.md states the authoring rule: a recommendation is never offered without its reasoning and why it beats the alternatives.

## Implementation notes

- The deleted-branch reconciliation resolves toward `/retro`'s positive-verification rule.
- Consuming repos may still carry the removed config key; the config read contract keeps unknown keys inert, so no settings migration is needed.

## Deviations

_None yet._
