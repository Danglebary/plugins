# Retro: Rename to pirr

## Problem — Exact match

The generic-name framing held; nothing in implementation challenged the case that `agentic-flow` was low-signal and baked into every surface a user and a coding agent touches. The rename reached exactly the surfaces the Problem enumerated — plugin directory, the `pirr:` invocation namespace, the `.pirr/` config directory, the manifests, the Glossary, the store contract, every skill and reference doc, the plugin-shipped agents, and the frozen specs/tickets/retros/ADRs.

## Goals — Exact match

All seven goals landed and were verified (ticket 001): the `pirr` plugin identity and marketplace source, `pirr:setup` and `pirr:refactor`, the `pirr:` agent namespace, `.pirr/` config with a regenerating generator template, the diff-materialization bats suite green, a case-sensitively-confirmed zero residual over tracked files, the backronym recorded once in the README and `CONTEXT.md`, and the `.agentic-flow/` → `.pirr/` migration note grounded against a real consumer repo.

## Non-goals — Exact match

Every non-goal held — branch names (`spec-*` / legacy `prd-*`) untouched, no immutable `<NNN>` renumbered, neither renamed skill re-scoped, no auto-migration logic added to `/setup`, and the untracked scratch tree left alone. ADR 0003 kept its number and every by-number cross-reference; only its filename slug changed, as planned.

## Approach — Extended

The ordered most-specific-first sweep, the atomic namespace-and-directory landing, the generator-template edit, and the bats-suite-as-acceptance-signal all held exactly. The extension the sweep surfaced (ticket 001): a case-sensitive token grep proves the *token* gone, not the *identity* — reaching "one canonical name" took a meaning-level pass beyond the mechanical sweep, one that both catches display-title and proper-noun variants the token pattern structurally can't match (only the spec-conformance judge caught the spaced, capitalized `# Improve Codebase Architecture` H1) and separates the deliberate keeps (the historical `Agentic-Flow` Notion page name) from the actual misses.

## Modules touched — Divergence

Every planned surface landed, but one module relocated instead of renaming in place (ticket 001): the historical review doc moved to a new dated `docs/reviews/2026-07-08-whole-plugin-deep-dive.md` home rather than an in-root `pirr-review.md`, so the archival name signals a superseded point-in-time snapshot where a root-level current-assessment name would mislead — with its one frozen cross-reference repointed so it would not rot.
