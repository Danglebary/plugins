# Retro: Rename to pirr

## Ticket 001 — Rename the plugin from agentic-flow to pirr across its committed surface

**Outcome**: Extended

A case-sensitive token sweep plus a zero-residual grep proves the *token* is gone, not the *identity*: the two real survivors were same-concept variants in other lexical forms — a spaced, capitalized skill H1 (`# Improve Codebase Architecture`) and a historical `Agentic-Flow` Notion page name — and only the spec-conformance judge, reading for meaning, caught the H1 that the token pattern structurally could not match. When a rename must reach "one canonical name," budget a meaning-level pass for display titles and proper-noun variants alongside the mechanical token sweep, and separate the deliberate keeps (the historical page name) from the misses (the title).
