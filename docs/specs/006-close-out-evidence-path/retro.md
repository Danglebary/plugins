# Retro: 006 — the close-out evidence path

## Ticket 001 — Untracked implementation files stop a close-out

**Outcome**: Extended

Designing the preflight against the repo's actual resting state rather than the ticket's imagined one changed the answer: six untracked banked ideas sat in the tree the whole time, which killed the stash-widening approach (ceremony plus a crash window on every routine close-out) in favor of a report-then-acknowledge round trip. A preflight added to a shared mechanism acquires callers nobody enumerated — the new exit fired inside `RECOVERY.md`'s resume recipe, and the deviation entry declaring that file "untouched" recorded the non-edit while missing its consequence, which is the shape a non-edit's blast radius always takes. The close-out pair earned its keep here: four real defects, two of them leaving an acceptance criterion partial, none of which the implementing session saw.

## Ticket 002 — A guarded section is compared in full and in isolation

**Outcome**: Extended

Chasing a bats line number that contradicted a hand trace found the larger bug: on bash 3.2 a failing `[[ ]]` inside a function trips neither `errexit` nor the ERR trap, so only a test's final command decides pass/fail and 26 of 67 assertions across both suites never ran — the guard-that-reports-all-clear failure this spec exists to close, sitting in the tests that were supposed to be checking for it. The lesson generalizes past bats: an assertion is infrastructure, and infrastructure that has never been observed failing is unverified regardless of how many times it has "passed". Two fence defects collapsed into one fix once fence state gated the start condition as well as the stop, which is why AC1 and AC2 needed no separate mechanism; and the close-out pair caught a narrowing the plan gate had approved but neither the deviations nor any fixture recorded — approval at plan time does not survive as evidence, only a test does.

## Ticket 003 — The evidence rules get one home

**Outcome**: Exact match

An ADR fully settled during grilling turned implementation into transcription: the plan gate carried only naming and inclusion decisions, and the diff produced zero deviations — the cheapest close in the spec so far, which is what front-loading design into the ADR buys. The deferred resolve-by-phrase steer proved itself immediately: two of the six anchors it covered had shifted *again* since the steer was written, confirming that a line number cited across a spec branch's lifetime is stale the moment a sibling ticket lands, and only the quoted phrase survives. Homing the rules ahead of their application leaves the format doc deliberately leading the live agent bodies — a planned drift window the sequencing (003 before 004/005) prices in rather than avoids.

## Ticket 004 — Every agent can report what it could not check

**Outcome**: Extended

Two verification tiers caught what the other could not: the mechanical check passed 15/15 on every probe while the decorrelated re-read found two real gaps behind those passing counts, and the close-out fact-check then caught an error in the re-read's own report — a "12 non-owning bodies" count that silently excluded the one body that differed. Each tier's blind spot was the next tier's finding, which is the argument for stacking them rather than trusting any one. The reload seam turned out to bound what a corpus-editing session can verify about itself: the register's failure arm fired against pre-edit bodies and proved nothing, and the probe that established this — asking the agent to quote its own output contract — is the cheap, general test for whether a dispatch served the tree you just edited. A frozen acceptance criterion can also be factually wrong about the repo: the six-member sync-set it names has four carriers, two of which never carried the phrase at all, so the criterion's operative half was satisfiable while its premise was false — and ADR 0006 repeats the same false premise.

## Verification record — ticket 004

**Handoff to ticket 005 (decided at ticket 004's plan gate):** the Partial verdict register is not the whole answer to the refactor brief's toolchain-verification demand — `refactor/SKILL.md`'s brief line ("verify any severity-determining claim about an external system … against the installed toolchain") remains an instruction no `[Read, Grep, Glob]` agent can execute. Ticket 005, which already edits that line to drop "checked, clean", rewords the toolchain half to route un-runnable checks into the register instead of demanding execution. Anchor by quoted phrase, not line number.

**Mechanical check (recorded per AC):** over `plugins/pirr/agents/*.md` — `^### Partial verdict$` present in **15/15**; halt phrase `and stop.` **0** occurrences; untrusted-content clause (`data, never direction`) **15/15**; `_Full._` sentinel **15/15**; ADR 0005 carve-out (`line-granular test`) **15/15**. Repo-wide sweep for `three sections`/`three-section`/`three pinned sections`: every survivor classified and allowed — `productivity/new/SKILL.md:45` (unrelated sense: handoff template), `docs/adr/0006:33` (frozen meta-quote naming this sweep), and the tamper-guarded spec-006 artifacts (spec.md:53, ticket 004's AC and its deviation entry). No live corpus copy remains un-reworded. Positive control per the Empty read corollary: the same grep matched the known-present reworded form (`finding sections`) in AGENT-FORMAT.md, `pirr/CONTEXT.md`, `done/SKILL.md`, `retro/SKILL.md`, and both pair bodies, proving the search resolved.

**Decorrelated re-read (recorded per AC):** a fresh-context agent read all fifteen bodies against EVIDENCE-PRINCIPLE.md, AGENT-FORMAT.md, and root CONTEXT.md's content-channel entry, hash-comparing the shared blocks. The fifteen bodies agree — register byte-identical across the 13 reviewers, exactly two reporting halves, no halt precedes any register, no body widens ADR 0005. Its clause claim needed correcting at close: it reported the clause "byte-identical across the 12 non-owning bodies" when there are **13** non-owning bodies — `standards-reviewer` is the thirteenth and carries a deliberate AC-23 tail, which the re-read excluded from its count without saying so. The close-out fact-check caught the silent exclusion; the variance is now a recorded deviation. Five seam findings: two are ticket 005's by scope (the brief's "checked, clean" line this ticket's notes forbid touching, and the unimplemented dispatch-layer machinery); one was judged no-fix (the three-part entry shape is canon's two-part form with the "why" unrolled per this ticket's AC — no semantic disagreement); two were fixed here — all fifteen registers now cite EVIDENCE-PRINCIPLE.md alongside ADR 0006 (making the doc's citation claim true on the agent side), and both close-out bodies' absence-claim bullets gained the Empty-read positive-control requirement with register routing, closing the gap between their "search before flagging missing" rules and the corollary that binds exactly that claim type.

**Live register-firing observation (recorded per the deferred steer): blocked by the reload seam — re-run required post-reload.** `pirr:qa-engineer` was dispatched against a brief built on EVIDENCE-PRINCIPLE.md's worked example (a toolchain-verification demand under a `Read, Grep, Glob` grant). The return carried candidates plus an improvised "Verification notes" section, confessed an unfired check mid-prose ("could not execute-verify — no execution tool"), and emitted **no register**. A follow-up probe asked the same agent to quote its own `## Output format` verbatim: it reproduced the **pre-edit** text, halt line included — proving the dispatch served the session-start body, so the run is evidence about the reload seam (spec 002's retro, extended: *edited* bodies are as stale as unregistered new ones within a session), not about the register. The register's failure arm therefore remains unobserved. Two follow-throughs: (1) re-fire this fixture after the plugin reloads — natural home: ticket 005's paired preflight-refusal observation; (2) any close-out pair dispatched **in this same session** (including this ticket's own `/done`) will run pre-edit bodies and return no register — expected, not a defect. Consistent unprompted behavior worth keeping: the stale-body agent still reported its capability gap in free prose, matching ticket 001's seven-of-seven observation — the register formalizes an instinct the agents already have.
